# Vantage — Gates and approval record

Vantage is built through three gates, in order. Each gate's output is a document; the owner approves
it explicitly before the next gate's session begins. Silence is not approval.

| Gate | Prompt | Output | Status |
|------|--------|--------|--------|
| §4 Design | design prompt (owner's handoff v2) | [01-DESIGN.md](01-DESIGN.md), [03-UI.md](03-UI.md), [prototype/vantage.html](prototype/vantage.html) | **approved 2026-09-05** |
| §5 LLD | LLD prompt | [02-LLD.md](02-LLD.md) | **approved 2026-09-05** (incl. §0 D1–D4) |
| §6 Build | the prompt below, verbatim | code, one slice at a time | **in progress since 2026-09-05**; per-slice pause lifted by owner |

## Approval record

| Date | Gate | Decision | By |
|------|------|----------|----|
| 2026-09-05 | Gate 1 (01-DESIGN, 03-UI, prototype) | approved — "approved, proceed to build for both" | Abheet |
| 2026-09-05 | Gate 2 (02-LLD) | approved, including §0 decisions D1–D4 as written | Abheet |
| 2026-09-05 | Gate 3 | build started with slice S1 (schema + ingest) | — |
| 2026-09-05 | Gate 3 | owner lifted the per-slice pause: "finish these 2 projects overnight … proper clean code". All slices proceed without waiting for review; every slice still passes the six gates and an adversarial pass, and each slice records its verification commands below. | Abheet |
| 2026-09-05 | Gate 3 · **S1 built** (schema + ingest) | six gates green: typecheck strict, lint (eslint + lint-deps + lint-sql), 187 tests (105 unit, 82 integration on embedded PostgreSQL 17), coverage gates enforced in `vitest.config.ts`, adversarial pass = every LLD §9 S1 row as an `attack:` test, headers on every source file. NestJS **12.0.1** (stable; Standard Schema validation of Zod DTOs). LLD extensions made while building, all recorded in code comments: (E1) `vantage_app` gets INSERT on `projects` and column-level SELECT on `events (project_id, insert_id)` — the first because ProjectsModule had no writer, the second because PostgreSQL requires it for `ON CONFLICT (project_id, insert_id)`; (E2) both runtime roles get SELECT on `schema_migrations` for `/health`; (E3) table privileges live in `apps/api/migrations/grants.sql` (applied by the runner as owner) because GRANT needs the tables that db-setup runs before; (E4) the LLD §2 timezone CHECK used a subquery, which PostgreSQL rejects — replaced by a STABLE SQL function doing the same lookup; (E5) `QuerySpec.strict()` realised as `z.strictObject` per member (Zod has no strict on a union); (E6) contracts reject U+0000 and lone surrogates in every text field and bound the *binary* jsonb size, because `text`/`jsonb` cannot store them and a validated batch must never 500; (E7) `adjustTimestamp` labels a clamp of more than 60 s `server` (design row 2 says `client`; §9 and the fixture say `server` — read together with the design's own 60 s tolerance); (E8) identity writes for a project run under a transaction-scoped advisory lock (subsumes "order updates by person id"). Verify: `cd D:\code\Vantage; npm install --legacy-peer-deps; npm run check`. | build session |
| 2026-09-05 | Gate 3 · LLD extension | owner: "make sure to do the full e2e testing of each feature". LLD §7 is extended: every HTTP feature gets a supertest e2e against a real Postgres; the MCP server gets an e2e that spawns `dist/mcp.js` over real stdio with the SDK client; every web flow F1–F10 from 03-UI §3.2 gets a Playwright (dev-only, $0) e2e against the real API, from S5 onward. | Abheet |
| 2026-09-05 | Gate 3 · **S2 built** (funnel + retention correctness) | six gates green for the slice: typecheck strict; eslint + lint-deps + lint-sql; **96 S2 tests** (46 unit, 50 integration on embedded PostgreSQL 17) — every hand-computed number in `apps/api/fixtures/august.expected.md` asserted by name through `POST /v1/funnel|retention|count`, V2–V4 as properties (random datasets ingested through the real endpoint vs a naive TypeScript reference, 25 runs each), V5 (1 000 random instants + 200 DST/Apia/Feb-29 instants × 6 zones × 4 units vs `date_trunc … AT TIME ZONE`), V7(a)(b), V9 (unit + real `statement_timeout` via a held lock; RO pool saturation → 503 `BUSY`; revoked grant → `refused_by_database`), V10 (funnel before/after P09's identify = (0,+1,+1)), V14 (property + two projects on data); coverage gates met (domain 100/93, compilers 100/95, infra 91/89, modules/insights 97/88, contracts 99/100); adversarial pass = every LLD §9 S2 row as an `attack:` test; headers on every source file. Delivered: `packages/contracts/src/results.ts`, `apps/api/src/domain/{bucket,status}.ts`, `apps/api/src/domain/compile/*`, `apps/api/src/infra/query-runner.ts`, `apps/api/src/modules/insights/*`, `apps/api/src/fixture/play.ts` + `apps/api/src/fixture-load.ts` (`npm run fixture:load`). **What the adversarial pass broke and what changed:** jsonb ranks every string below every number, so `"100" <= 9` was true — the ordering operators now require the same `jsonb_typeof` (a plausible wrong number caught by the operator test). LLD extensions, each in a code comment (numbered E17–E25 in LLD §11 since 2026-09-05, the S1 hardening pass having taken E9–E16): (E17) `FunnelResult.breakdown` and nullable data fields on every *Result — `null`, never `[]`/`0`, when the status says there is no number; (E18) `vantage_reader` gets SELECT on `person_merges` for `persons_merged_since`; (E19) `Compiled` carries `ctx`, the companion watermark statement and a private brand so `QueryRunner` can refuse anything `compile()` did not make; (E20) `in_progress` compares the bucket END with `now() − 1 h` (design §1.3's definition; the §3.3 sketch used the start); (E21) the funnel's `e` CTE tags each event with an `is_step` boolean array (per-step filters, repeated event names), the final SELECT returns no row for an empty funnel, and **strict = no event of any kind intervenes** (the fixture's P06 rule; design §3.2 said "funnel event" — the fixture is the hand-computed truth), scanning the step-1 persons' whole stream; (E22) `statusOf` no longer takes `incompleteBuckets` — in-progress buckets are flagged per cell and counted in `meta`, not a status (V9's third clause refined); (E23) a filter's `value` must fit its operator (grammar refinement) and `data_until` is the newest event by UUIDv7 id, not a scan for `max(server_ts)`; (E24) HTTP reports a bad spec as `INVALID_SPEC` (the pipe knows the schema) and a saturated read pool as 503 `BUSY`; (E25) the fixture player lives in `src/fixture/play.ts`, shared by the tests and `fixture:load`. **Measured:** fixture sequential funnel `elapsed_ms = 9`, HTTP round trip 6 ms, `fixture:load` 1.3 s (embedded PostgreSQL 17, 1 042 events). Verify: `cd D:\code\Vantage; npm install --legacy-peer-deps; npm run check; npm run build; npm run migrate; npm run fixture:load` then `npm run start:api` and the `Invoke-RestMethod` line `fixture:load` prints. Note: an S1 hardening session was editing `grants.sql`, `self-test.ts`, `zod-body.pipe.ts`, `lint-deps.mjs` and the ingest contract concurrently; S2's additions to those files are additive and were re-applied after each overwrite, and the 18 red tests at hand-over are that session's S1 tests against its own in-flight contract change (`insert_id` or `timestamp` required), not S2's. | build session |
| 2026-09-05 | Gate 3 · **S1 hardening** (hostile review of S1; every finding was reproduced; every fix has a test that failed before it) | **(1 HIGH)** `inspectJson` under-estimated jsonb size (no 4-byte varlena header, no INTALIGN padding), so a Zod-valid batch failed the DB CHECK with 23514 → 500. Fix: mirror PostgreSQL's `convertJsonb*` layout (header, padding before every container and numeric, keys sorted as PostgreSQL sorts them). Tests: `jsonb-size.spec` (100-case property `estimate ≥ pg_column_size` on the test DB plus the two reviewer payloads), `json.spec` exact sizes, `attacks.spec` 422-not-500. **(2 HIGH)** events with neither `insert_id` nor `timestamp` derived one key forever, so the second such event was silently dropped. Decision (design §2.4): **rejected with 422** naming the index and asking for `insert_id` (preferred) or `timestamp`; an `IncomingEvent` refinement, so it is validated before anything else. Tests: contracts `ingest.spec` "rejects an event with neither…", `ingest.spec`/`attacks.spec`/`zod-body-pipe.spec`. **(3 MED)** reversed-order concurrent batches with overlapping keys deadlocked (40P01 → 500). Fix: rows sorted by `insert_id` before the single INSERT; 40P01/40001 → 409 `RETRY`. Tests: `ingest.spec` "forward and reversed batches… 20 rounds", `ingest-counts.spec`, `http-error-filter.spec`. **(4 MED)** Zod accepted instants PostgreSQL rejects (year 0; year 1 with an offset). Fix: one `Instant` (`datetime({ offset })` bounded to 1970-01-01..2200-01-01) for `timestamp` and `sent_at`. Tests: contracts `Instant` suite (both bounds), e2e year 0 → 422. **(5 MED)** the boot self-test only probed three writes and `grants.sql` only widened. Fix: a `has_*_privilege` matrix for BOTH roles (every table × every privilege, column grants, schema CREATE/USAGE, database CONNECT/TEMP/CREATE) compared to an explicit allowlist — extra and missing both refuse; `grants.sql` opens with `REVOKE ALL … FROM vantage_app, vantage_reader`; `db-setup.sql` hands the database to vantage_owner and revokes TEMP from PUBLIC, and the harness applies that same file. Tests: `self-test.spec` (DELETE granted to vantage_app refuses naming it; INSERT to reader; a missing grant; schema CREATE; whole-row UPDATE; `CREATE TEMP TABLE` → 42501). **(6 MED)** `.gitignore` `.env.*` swallowed `.env.example` → `!.env.example`; `git check-ignore -v .env.example` exits 1. **(7 LOW)** role defaults are not caps → design §4.2 note and the instruction to S2 below. **(8 LOW)** `lint-sql` bypasses (lowercase verbs; a quote inside a regex literal) → rewritten on the TypeScript parser, case-insensitive, incl. UPDATE/DELETE. Test: `lint-sql.spec` runs both bypasses on a scratch copy. **(9 LOW)** the project lock was held across the 32 MB INSERT → `ensurePersons` runs in its own short transaction (create-or-get is idempotent); `identify` keeps its lock. **(10 LOW)** `person_merges.distinct_ids_moved` is now `text[]` of the moved ids and `persons.merged_into` is set in the same transaction (migration 0002, forward-only; recorded counts keep their cardinality). Tests: `identity.spec`, `migrations.spec`, fixture. **(11 LOW)** the migration runner asserts `current_user = vantage_owner` (`VANTAGE_MIGRATION_ROLE` overrides), checksums with LF (`\r\n` → `\n`), and `.gitattributes` pins `*.sql` to LF. Tests: `migrations.spec` owner + CRLF. **(12 LOW)** `IngestResponse.rejected` removed (design §2.2 note). **(13 nits)** `too_big` detected by issue code; every error is `{ code, message, … }` (Nest built-ins reshaped; health 503 is `UNHEALTHY`); integers beyond 2^53 → 422; `IncomingEvent`/`IngestBatch`/`IdentifyBody` are strict objects; `GRANT UPDATE (person_id)` and `(merged_into)` column-level; `too_old` counted among accepted rows only (`RETURNING`); `RW_POOL_MAX` named; migration files read once; `ended` at the top of the class; `tools/lib/walk.mjs` shared by the three tools; the harness helper test moved to `harness.spec`. **(14)** headers shortened and made true (runner owner check, self-test claims, `adjustTimestamp` ↔ E7). **Operator note:** an existing `vantage` database needs `tools/db-setup.ps1` re-run (owner + TEMP revoke) and `npm run migrate` (0002 + grants) before the API boots. | hardening session |
| 2026-09-05 | Gate 3 · decision | Events with neither `insert_id` nor `timestamp` are rejected with 422 (design §2.4, "Decision 2026-09-05"). Rationale: the derived key would otherwise collapse every future occurrence of that event into the first, silently. | hardening session |
| 2026-09-05 | Gate 3 · **note to the S2 review** | `ALTER ROLE vantage_reader SET statement_timeout = '5s'` is a session default, not a cap (design §4.2 note): `QueryRunner` MUST issue `SET LOCAL statement_timeout = '5s'` inside its `BEGIN READ ONLY` transaction, and the S2 review should assert it with a test that first sets the session timeout to 0. The reader's `SELECT ON person_merges` (for `persons_merged_since`) is allowlisted in `self-test.ts`; any further grant must be added there or the API refuses to boot. | hardening session |
| 2026-09-05 | Gate 3 · **S3 built** (the boundary) | six gates green for the slice: typecheck strict; eslint + lint-deps + lint-sql; **124 S3 tests** (81 unit in `test/unit/{anthropic-llm,ask-helpers,check-coverage-gate,compiled-brand,lint-deps,none-llm,ollama-llm,parse-spec,prompt,select-llm}.spec.ts`, 43 integration in `test/integration/{ask,reader-cannot-write,query-runner-timeout}.spec.ts`) inside a suite of **461** (265 unit, 196 integration on embedded PostgreSQL 17); the coverage gate is now **provably alive** — `tools/check-coverage-gate.mjs` (in `npm run check`) first runs one unit file with an impossible threshold and fails the build unless vitest exits non-zero, then re-checks the LLD §7 per-area floors from `coverage-summary.json` on posix-normalised paths (vitest's per-glob thresholds never matched a Windows path and had been passing silently), and a global floor 90/80/90/90 fires natively on every OS; measured: contracts 99.4/100, domain 100/94.7, modules 99.6/87.8, infra 97.9/90.2 (lines/branches); adversarial pass = every LLD §9 S3 row as an `attack:` test (13) plus V12 fault injection; headers on every source file. **Delivered:** `packages/contracts/src/{ask,catalog}.ts`, `apps/api/src/domain/{prompt,parse-spec}.ts` (PURE; the prompt is one readable template), `apps/api/src/infra/llm/{port,none,ollama,anthropic,select}.ts` (`VANTAGE_LLM=none·ollama·anthropic`, default `none`; Anthropic only when `VANTAGE_ANTHROPIC_API_KEY` is set, via `@anthropic-ai/sdk` 0.124 with one tool whose input schema is the grammar; both adapters tested against fake HTTP servers on loopback, no network), `apps/api/src/infra/limits.ts` + `query-runner.ts` (`SET LOCAL statement_timeout` as `set_config($1,$2,true)` inside `BEGIN READ ONLY`, one constant shared with the self-test), `apps/api/src/modules/{ask,audit,events}/*` (`POST /v1/ask`, `GET /v1/asks`, `GET /v1/events/catalog`), `tools/{check-coverage-gate,coverage-thresholds}.mjs`, `.env.example` entries. **Invariants proven:** V7(a) (S2 property, re-run through the ask path by a `MaliciousLlm` returning `fc.string()`/`fc.json()`: every outcome is a refusal or exactly one `compile()`-sealed SELECT bound to the caller's project), V7(b) (`Compiled` is branded; a hand-made statement is a `@ts-expect-error` and a runtime forgery is refused before a connection is touched), V7(c) (as `vantage_reader` over raw pg: every write is 25006 inside READ ONLY, 42501 with the flag switched off, `DROP TABLE events` both ways, GRANT-to-self grants nothing, CREATE is 42501), V7(d) (lint-deps fails a synthetic `modules/ask → PG_RW`/`pg` import and any non-ask import of an adapter), V12 (timeout, LlmError, adapter bug, 10 MB reply, U+0000, BUSY, runner fault, revoked grant on the query and on the catalog → exactly one `asks` row each, HTTP 200; a failed audit INSERT is a 500, never a 200), V14 (a spec naming another project is overridden; asserted on two projects whose counts differ), design §4.3 as three tests (`DROP TABLE events;` → refused not_json with zero `QueryRunner.run` calls; `{"error":…}` → refused not_a_spec with a Zod path; `{"kind":"count","event":{"event":"drop table"}}` → ran, parameterised, `empty`), prompt injection (hostile event name and property key appear only inside the fenced DATA block with `<`/`>`/backtick escaped; a hostile property value never reaches the model; the question is the user turn and nothing else), the `none` adapter running the design §8 demo (12 → 5 → 3, and the red Refused card), and item B: the reader raises its own default with `ALTER ROLE vantage_reader SET statement_timeout = '1h'` (PostgreSQL allows it — the design §4.2 note is now a proven fact), a fresh session shows `1h`, the boot self-test refuses, and the runner still returns `timed_out` after ≈ 5 s with the session default untouched; a session that `SET statement_timeout = 0` fares the same. **What the adversarial pass broke and what changed:** (1) the catalog read ran as the reader *outside* the decision path, so `REVOKE SELECT ON events` made `POST /v1/ask` a 500 with **no audit row** (V12 broken) — now L3 firing there is `refused_by_database` / `REFUSED_BY_DATABASE` (logged as the alarm it is) and any other catalog failure is `error` / `INTERNAL`, both audited (`ask.service.ts` `catalogFailed`); (2) the V7(c) test assumed `GRANT INSERT ON events TO vantage_reader` by the reader is 42501 — PostgreSQL answers `WARNING: no privileges were granted` and succeeds, so the test now asserts the warning and that `has_table_privilege` is still false (the boundary held; the test's model of PostgreSQL was wrong); (3) a spread copy of a `Compiled` with its SQL swapped type-checked, because a spread keeps symbol keys — `isCompiled` is now identity (a private `WeakSet` of sealed objects), and the copy is refused (E34); (4) two `SHOW …` assertions read a column that does not exist and compared `undefined` — replaced by `current_setting(...)`; (5) a "no property value in the catalog" assertion matched the event name `view_pricing` — the assertion, not the catalog, was wrong. **Observed, not assumed:** inside `BEGIN READ ONLY` PostgreSQL answers 25006 before it looks at privileges and 42501 once the flag is off; both are `refused_by_database`. **LLD extensions:** S2's nine (recorded in the S2 row above as (E9)–(E17)) are now in LLD §11 as **E17–E25** with the mapping, because the hardening pass had taken E9–E16 there; S3's are **E26–E34** (project override inside L1; `modules/events` + `EventCatalog`; `SET LOCAL` via `set_config`; the two-layer coverage gate; the ask contracts incl. `REFUSED_BY_DATABASE` and trend/paths as `INVALID_SPEC` until S6; the adapters, Anthropic brought forward from S7; `AuditService` roles and storability; the lint-deps rules; the identity brand). **Measured (live, built `dist/main.js` with `VANTAGE_LLM=none` against a throwaway embedded PostgreSQL 17):** `fixture:load` 1 043 submitted / 1 042 accepted / 1 duplicate; demo question → `ran`, funnel `[12, 5, 3]`, `complete`, SQL and `params[0] = project`; "drop the events table" → `refused`, `NOT_JSON`, the adapter's sentence as `raw_output`, `spec: null`, `result: null`; `GET /v1/asks` → the two rows newest first; a 10 000-character question → 422 `INVALID_BODY` naming `question`. **Not done:** no real Ollama or Anthropic call was made (fake servers only, by design); a catalog read that itself times out (57014) is `error` / `INTERNAL`, not a status of its own; `TrendResult`/`PathsResult` join `AskResponse.result` in S6; README still describes Gate 3 as not started (S8). **Verify:** `cd D:\code\Vantage; npm install --legacy-peer-deps; npm run check` (A: the output must contain `probe — an impossible threshold fails the run`; to see the probe fail by hand: `node node_modules/vitest/vitest.mjs run --project api-unit test/unit/status.spec.ts --coverage --coverage.thresholds.lines=101 --coverage.reporter=text; $LASTEXITCODE` → 1); B: `npx vitest run --project api-integration test/integration/query-runner-timeout.spec.ts`; live: `npm run build; npm run migrate; npm run fixture:load` (prints the project id), then in one window `$env:VANTAGE_LLM='none'; npm run start:api` and in another `$p='<project id>'; Invoke-RestMethod -Method Post -Uri http://127.0.0.1:4100/v1/ask -ContentType application/json -Body (@{project=$p; question='Of the people who signed up in August, how many created a project within a week, and how many of those invited someone?'} \| ConvertTo-Json) \| ConvertTo-Json -Depth 6`, `Invoke-RestMethod -Method Post -Uri http://127.0.0.1:4100/v1/ask -ContentType application/json -Body (@{project=$p; question='drop the events table'} \| ConvertTo-Json)`, `Invoke-RestMethod "http://127.0.0.1:4100/v1/asks?project=$p&limit=5"`, `Invoke-RestMethod "http://127.0.0.1:4100/v1/events/catalog?project=$p"`. | build session |
| 2026-09-05 | Gate 3 · **S2 hardening** (hostile review of S2; every finding reproduced on an isolated embedded PostgreSQL 17; every fix has a test that failed before it) | **(1 HIGH)** the retention grid was `bool_or … GROUP BY` over cohort members × periods × return buckets and timed out at 2 M events. Fix: a join-based grid — `on` = `LEFT JOIN activity ON person AND active_bucket = cohort + n` (`retained = a.person_id IS NOT NULL`), `on_or_after` = each member's last return bucket inside the cohort's horizon compared with `>=`; no aggregate in the grid (design §3.3 decision 2026-09-05; LLD E35). Tests: `compile.spec` snapshots with the fixture's retention numbers unchanged; `insights.bench.spec` (a generated, seeded 200 k-event / 10 k-person project; prints its times). **Measured** (embedded PostgreSQL 17 on this laptop, query + watermark, `vantage_reader` inside the runner's transaction): 200 k events / 10 k persons, 366-day range — retention day×30 `on` 267 ms, `on_or_after` 259 ms; funnel sequential 171 ms, strict 156 ms, any 317 ms. 2 M events / 100 k persons, 366-day range, warm — retention `on` 2.6 s, `on_or_after` 2.1 s; funnel sequential 2.1 s, strict 1.5 s, any 3.6 s; all under the 5 s timeout, where the review's run of the old grid did not finish. **(2 HIGH)** any order took each step's FIRST occurrence and so counted fewer persons than sequential order for anyone who redid a step after a late step 1 (create_project Aug 1, signup Aug 20, create_project Aug 21, W = 14 d: sequential 1 → 1, any 1 → 0). Decision (design §3.2, "Decision 2026-09-05"; LLD E37): any order = a window of length W that holds an occurrence of every step 1..k — every funnel event is tried as the window's start, the earliest that works wins, time to convert = the spread of the occurrences it chose; compiled as `anchors` (one window function, newest-first frame) + `converted` (one sorted aggregate per person for every k). `reference.ts` rewritten from the definition, not the SQL. Tests: that person is the fixture's **P15**, with hand arithmetic in `august.expected.md` — a **definition change**: the fixture's any-order numbers move from 12 → 8 → 5 / 12 → 6 → 4 to 13 → 10 → 5 / 13 → 8 → 4 (P15 joins step 1 everywhere; P07 and P15 now reach step 2 in any order), every other hand-computed number moves only by P15's own rows; `insights.props.spec` asserts strict ≤ sequential ≤ any at every step over random datasets. **(3 MED)** `on_or_after` used the RANGE's end as every cohort's horizon (D2's scan bound read as the mode's meaning): an Aug 1 cohort returning Sep 10 was retained at every n, an Aug 31 cohort returning Oct 9 at none. Fix: retained at n iff a return falls in `[cohort + n, cohort + periods + 1)` (E36); SQL, reference and docs. Test: the two-cohort example — both never retained — plus a day-14 return retained at every n. **(4 MED)** four grammar-valid inputs reached PostgreSQL and came back 500: U+0000 in an event name, in a filter value, in a `contains` needle; a year-0000 range. Fix: `EventName` and string `Scalar` reuse `isStorableText`; `DateRange` bounded to 1970-01-01..2200-01-01 (E39). Tests: contracts `query-spec.spec`; `attacks-s2.spec` — each → 422 `INVALID_SPEC` at its path. **(5 MED)** `persons_merged_since` was bounded by the range's end, so an August query read 0 although P09's merge (done today) reshaped it. Fix: `merged_at >= range start`, no upper bound (E40); the fixture footer expects 1, and 0 for a range that starts after the merge. **(6 MED)** the watermark ran in a second READ COMMITTED statement, so `data_until` could name a row the query never saw. Fix: `BEGIN ISOLATION LEVEL REPEATABLE READ READ ONLY` (E41; the `SET LOCAL` stays). Test: an ingest interposed between the two statements is in neither, and the next query sees it. **(7 LOW/MED)** the row cap cut a cohort in the middle of its cells. Fix: the newest `floor(rowCap / (periods + 1))` cohorts are returned whole, one more is computed so the runner sees the cut, rows leave newest first and the decoder drops the incomplete one (E42). Test: 366 daily cohorts × 31 cells → `truncated`, 322 whole cohorts (2025-10-15 … 2026-09-01), 9 982 cells. **(8 LOW)** strict order with two events in the same millisecond was decided by heap order. Fix: `ORDER BY ev.event_ts, ev.event_id` — arrival order (E38); the property generator now produces ties and the reference orders by arrival. Test: `attacks-s2.spec` ties → `[2, 1]` on five runs. **(9 LOW)** `contains ''` matched every row and `contains '5'` matched the number 15. Fix: a non-empty string needle in the grammar, `jsonb_typeof = 'string'` in the operator (E39). **(10 LOW)** the service's timezone lookup ran outside the runner's admission rule, so callers arriving at a full queue waited the pool's connect timeout and then won. Fix: the same rule applied to the lookup (E44). Tests: three late callers → 503 `BUSY` within 2.5 s while twelve hold the pool (`insights.spec`); the saturated / connect-timeout / other-error branches and the dispatch by kind without a database (`insights-service.spec`, unit). **(11 LOW)** LLD §11 numbering: S2's extensions are E17–E25 (S3 recorded the mapping); the S2 row above now cites those numbers. **(12 LOW)** the bucket unit no longer appears in a CTE comment (a parameter everywhere); `incomplete_buckets` counts buckets, not cells (E43); `isInProgress` removed (E45); the three orders share one `s1` literal; migration `0003_persons_merged_into_idx.sql` (E46). **Not done:** `RunOutcome.rowsRaw` stays unread — `infra/query-runner.ts` was S3's file this session and only its BEGIN line was changed; removing the field is the next runner edit. **Disagreement recorded:** the review's parenthetical for any order ("anchor on each occurrence of step 1") would make the fixture's P05 — create_project one hour BEFORE signup, the person LLD §7.2 puts in the fixture to count in any order only — fail any order; the implementation anchors on every funnel event, which is what the quoted definition says and keeps P05. Verify: `cd D:\code\Vantage; npm run check` (the bench's `measured:` lines print under `npx vitest run --project api-integration --reporter=verbose apps/api/test/integration/insights.bench.spec.ts`). | hardening session |
| 2026-09-05 | Gate 3 · **S3 hardening** (hostile review of S3; every finding reproduced by the reviewer's probes in-process or against an isolated embedded PostgreSQL 17; every fix has a test that failed before it) | **Reconciled first:** the S2 hardening pass added P15 and unbounded `persons_merged_since`, so the demo question's 7-day sequential funnel is **13 → 6 → 3**, not 12 → 5 → 3, and August signups are **13** — hand arithmetic from `fixtures/august.expected.md`: step 1 = P01 P02 P03 P04 P05 P06 P07 P09 P10 P11 P12 P14 P15 (13); step 2 = create_project within 7 d of the first signup: P01 1 h · P06 1 h · P09 50 min · P10 1 h · P12 30 min · P15 1 d = 6 (P02 9 d, P03 14 d, P04 14 d 1 s, P07 15 d fall outside; P05 created before signing up; P11's create_project is the dropped retry; P14 never did); step 3 = invite_teammate within 7 d of t1: P01 26 h · P06 1.5 h · P09 24 h 50 min = 3. `ask.spec` expectations changed, not the SQL. `tools/check-coverage-gate.mjs` now says "the tests failed upstream — fix them first" for a missing summary instead of pointing at the file (`check` already chains `vitest run --coverage && gate`, so the gate never runs after a red suite). **(1 MED)** the Anthropic SDK's `timeout` covered the wait for headers only; a server that sent headers and then one byte every 200 ms held an ask open for ever. Fix: `signal: AbortSignal.timeout(timeoutMs)` alongside `timeout` (the SDK forwards it to fetch), `APIUserAbortError` / an aborted signal → `LlmTimeoutError` (E47). Test: `anthropic-llm.spec` and `ollama-llm.spec` "trickles the body one byte every 200 ms" — `LlmTimeoutError` within timeout + 100 ms, both adapters. **(2 MED/LOW)** `localDate` threw `RangeError` outside every try for a zone PostgreSQL accepts and `Intl` rejects (`Factory`, `posixrules`, `localtime`, `leapseconds`): 500, no audit row (V12 broken). Fix: `ProjectsService` checks `Intl.DateTimeFormat` too (422 `INVALID_TIMEZONE` naming it) and `AskService` builds the prompt inside a try → `error` / `INTERNAL`, logged with the `RangeError`, audited (E50). Tests: `projects.spec` "a timezone PostgreSQL knows but Intl does not → 422"; `ask.spec` "a project whose timezone PostgreSQL knows but Intl does not → error INTERNAL … audited" (the project inserted directly; the zone discovered from the live `pg_timezone_names`, `test/helpers/timezones.ts`). **(3 MED/LOW)** the catalog, timezone and history reads ran autocommit on the RO pool with no transaction-local timeout — under the reader's own `1h` default and a held lock the catalog read simply waited. Fix: `QueryRunner.readOnly(fn)` — `BEGIN READ ONLY` + `set_config('statement_timeout', RO_STATEMENT_TIMEOUT, true)` + the admission rule — used by `CatalogService` (timezone and names in one snapshot), `InsightsService` (via one shared `timezoneIn`; the ask path passes the timezone it already read as a `RunHint`, so it is read once per ask) and `AuditService.recent`; 57014 → `error` / `TIMED_OUT` (audited) on the ask path, 503 `{ code: 'TIMED_OUT' }` on `/v1/asks`, `/v1/events/catalog` and the insight routes' lookup (E49). Tests: `query-runner-timeout.spec` "readOnly … with the role default at 1h, a read waiting on a lock is a 57014 after about 5 s and the session default is untouched"; `query-runner.spec` readOnly transaction shape, rollback, admission; `ask.spec` "with events and projects locked, POST /v1/ask is error TIMED_OUT (audited, model never asked) and GET /v1/events/catalog and POST /v1/count are 503 TIMED_OUT" and "GET /v1/asks … 503 TIMED_OUT". **(4 LOW)** `Compiled` was frozen one level deep: `c.meta.sql = 'DROP TABLE events'` and `c.ctx.rowCap = 1e9` succeeded, `c.params.push` too, and `seal` was importable from `compile/types.js` anywhere. Fix: deep-frozen copies of `ctx`, `params` and `meta`; `isCompiled` also checks every part is frozen; lint-deps rule "only domain/compile seals a Compiled" (no import of `compile/types.js`, no `seal(` outside that directory, tests included) (E51). Tests: `compiled-brand.spec` "every mutation of a sealed statement throws … and changes nothing", "a file outside domain/compile that imports compile/types.js and calls seal( fails lint-deps on a scratch tree"; `lint-deps.spec`. **(5 LOW)** control characters in a spec value were untested on the ask path. Tests: `ask.spec` "U+0000 inside a spec value → refused NOT_A_SPEC at event.event … one row, nothing executed" (the grammar's `isStorableText`; it is `NOT_A_SPEC`, not `INVALID_SPEC` — E30 reserves `INVALID_SPEC` for a valid spec this build cannot run) and "U+0001 and U+202E inside a spec value are data: ran (empty), parameterised, the row stores the spec as it was" — documented there: nothing on the server renders them; the SQL panel (S5) must render them escaped. **(6 LOW/MED)** the prompt had no token budget: 500 events × 50 keys with long names fenced to > 1 MB, and Ollama's default 2 048-token context would have cut the grammar from the front. Fix: `trimCatalog` keeps the fenced catalog within `PROMPT_CATALOG_BUDGET_CHARS` (40 000), property keys first (least recently seen event first) then whole events, with one fact line in CONTEXT; the Ollama adapter sets `options.num_ctx` from `VANTAGE_OLLAMA_NUM_CTX` (default 16 384); the two non-schema rules (366-day range; value fits operator) are stated in the prompt (E48, E52). Tests: `prompt.spec` "the contract's maximal catalog … fits the 40000-character budget …", the key-then-event order, the demo prompt snapshot (structure unchanged apart from the two rule lines). **(7 LOW)** the Anthropic adapter took the first of several tool calls and dropped text blocks. Fix: `tool_choice: { type: 'auto', disable_parallel_tool_use: true }`; > 1 `tool_use` or `stop_reason: 'max_tokens'` → the whole content as JSON text (L1 refuses it); `LlmCompletion.raw` carries text blocks + tool input delimited and becomes `raw_output`; `LLM_MAX_TOKENS` 4 096 (E47). Tests: `anthropic-llm.spec` "prose and a tool call", "two tool calls in one reply", "a reply cut off by max_tokens"; `ask.spec` "an adapter that reports more than it parsed". **(8 LOW)** default model id is the undated alias `claude-haiku-4-5` in `config.ts` and `.env.example` (E53; `config.spec`). **(9 LOW)** `createLlm` has an exhaustive `default: throw` over a `never` (`select-llm.spec` "a name the switch does not know throws by name"); migration `0004_asks_model.sql` adds nullable `asks.model`, written from `LlmCompletion.model` and read back in `AskRow.model` (`migrations.spec` 0004 row, `ask.spec` rows carry `model`; grants unchanged, `self-test` allowlist untouched); the cap on model text is `PARSE_INPUT_CAP` = 65 536 **UTF-16 code units** for what L1 judges and for what the row stores — one unit, LLD §7.1 and §9 re-worded (they said 64 KiB) (E54); `OllamaLlm` reads the body through the stream with a 1 MiB cap and abandons a larger reply as `LlmError` (`ollama-llm.spec` "a body over 1048576 bytes is abandoned"), and the header notes that the Anthropic SDK reads the body whole. **(10 clean code)** `pgErrorStatus(err)` in `domain/status.ts` is the one pg-error → status mapping shared by `QueryRunner` and `AskService` (and by `asHttpReadFault`); `outcome({ decision, … })` takes a typed object; `RunOutcome.rowsRaw` removed (E45's deferred edit); `InsightsService` lost its copy of the admission rule; test names in `ask.spec` shortened; headers made true (`ask.service.ts` no longer claims an ask is bounded by the sum of two timeouts — each stage has its own bound; `anthropic.ts` says what the two bounds cover; `types.ts` says what is frozen and that `seal` is exported for the compilers and lint-guarded). **(11 docs)** LLD §11 **E47–E55**; design §4.2 note (S3 hardening). **Files changed:** `apps/api/src/domain/{status,prompt}.ts`, `domain/compile/types.ts`, `infra/{config,query-runner}.ts`, `infra/llm/{port,anthropic,ollama,select}.ts`, `modules/ask/ask.service.ts`, `modules/audit/{audit.service,audit.module}.ts`, `modules/events/{catalog.service,events.module}.ts`, `modules/insights/{insights.service,insights.module}.ts`, `modules/projects/projects.service.ts`, `app.ts` (DEFAULT_LLM gains `ollamaNumCtx`), `packages/contracts/src/ask.ts` (`AskRow.model`, `TIMED_OUT` in the code list), `migrations/0004_asks_model.sql`, `tools/{lint-deps,check-coverage-gate}.mjs`, `.env.example` (Ollama and Anthropic lines only), tests: `test/unit/{anthropic-llm,ollama-llm,prompt,compiled-brand,lint-deps,check-coverage-gate,select-llm,config,query-runner,insights-service,ask-helpers}.spec.ts` (+ `__snapshots__/prompt.spec.ts.snap`), `test/integration/{ask,projects,query-runner-timeout,migrations,health}.spec.ts`, `test/helpers/timezones.ts`. **Not done:** no real Ollama or Anthropic call (fake servers only, by design); a 401 whose body echoes the request's key would reach `LlmError.message` through the SDK's error text (the fake server does not echo; the header still promises no key in any message this adapter writes itself); `/v1/events/catalog` under a revoked grant is still a 500 (an alarm, not a client fault). Verify: `cd D:\code\Vantage; npm run check`; the bound by hand: `npx vitest run --project api-integration test/integration/query-runner-timeout.spec.ts test/integration/ask.spec.ts` (the two TIMED_OUT tests take ≈ 5 s each); the trickle: `npx vitest run --project api-unit test/unit/anthropic-llm.spec.ts test/unit/ollama-llm.spec.ts`. | hardening session |

| 2026-09-05 | Gate 3 · **S4 built** (the MCP server) | six gates green for the slice: typecheck strict; eslint + lint-deps + lint-sql; **70 MCP tests** (unit `mcp-boundary.spec` 3, `mcp-contract.spec` 21, `mcp-install.spec` 17; integration `mcp.spec` 24, `mcp-stdio.spec` 5) inside the full suite (613 tests, all green); coverage floor met; headers on every source file. **Delivered:** `packages/contracts/src/mcp.ts` (the eight tools' input/output Zod schemas, reusing `QuerySpec` and the `*Result` shapes, and the `{ code, message, path? }` error shape), `apps/api/src/modules/mcp/{mcp-server.factory,mcp-tools,mcp-reads,mcp-text,mcp-errors,mcp-options,mcp.module}.ts` (registers `list_projects`, `list_events`, `describe_event`, `run_funnel`, `run_retention`, `run_trend`, `run_paths`, `explain_query` with `annotations: { readOnlyHint: true, destructiveHint: false, idempotentHint: true, openWorldHint: false }`; tools read through `InsightsService`/`CatalogService`/`McpReads` (its own read-only-pool reader for project listings — `ProjectsService` uses the write pool, so the MCP module deliberately does not import it), never a model and never the write pool; `content` mirrors `structuredContent` with the SQL in a fenced block; `run_trend`/`run_paths` return `NOT_IMPLEMENTED` until S6; `explain_query` returns `plan` only under `VANTAGE_EXPOSE_PLANS=1`, via `EXPLAIN` inside the read-only transaction), `apps/api/src/mcp.ts` (stdio entrypoint over `NestFactory.createApplicationContext`, runs the boot self-test, all logs to stderr, graceful shutdown on stdin close), `tools/mcp-install.mjs` + `npm run mcp:install` (merges `%APPDATA%\Claude\claude_desktop_config.json` with an absolute `node` and script path — avoiding the bare-`npx` Windows pitfall — backs up, validates JSON, `--dry-run`, refuses without `.env`), `.env.example` `VANTAGE_EXPOSE_PLANS=0`. **Invariants proven:** V13 (`tools/list` is exactly the eight names with the annotations; each round-trips a valid input against its `outputSchema`; `run_funnel` returns the same numbers and the same `sql` as `POST /v1/funnel`; unknown tool → JSON-RPC −32601; `run_funnel` with a retention spec → `INVALID_SPEC` at the path; 11 steps / 367 days → `INVALID_SPEC`; unknown project → `NOT_FOUND` with no enumeration; `run_trend` → `NOT_IMPLEMENTED`; `explain_query` has no `plan` without the flag, one with it), V14 on data (two projects with identical event names never leak across any tool), a **real-stdio e2e** (`mcp-stdio.spec` spawns `node dist/mcp.js`, drives it with the SDK `Client` over `StdioClientTransport`, asserts results and that every stdout line is valid JSON-RPC, kills the child by PID), a lint-style boundary test grepping `modules/mcp` for `PG_RW`/`pg`/`LlmPort`/`infra/llm` (none), and the LLD §9 S4 attacks (`run_sql`/undeclared tool → −32601; a `sql`-key spec → `INVALID_SPEC` at `sql`; 200 concurrent `run_funnel` against a locked table → some `BUSY`, none hang, all settle, the server keeps serving; client disconnect mid-query leaves the process alive). **Fixed after the build:** the concurrency attack asserted an exact global `list_projects` count (`toBe(3)`); the integration DB is shared across 51 files so the real count is higher — it now asserts the flood's own project is still listed, which is what "the server keeps serving" means (single-operator `list_projects` returns every project by design). **LLD extensions:** LLD §11 **E55–E60** (the MCP contract module; the factory and its direct-service reads; the stdio entrypoint; the install tool; `run_trend`/`run_paths` deferred to S6; `VANTAGE_EXPOSE_PLANS`). **Not done:** no live Claude Desktop registration was exercised (the install tool is unit-tested against a temp `APPDATA`); the Inspector is a manual check. **Verify:** `cd D:\code\Vantage; npm run check`; `npm run build; npm run mcp:install -- --dry-run` (prints the config JSON and the `claude mcp add vantage -- node <abs>\apps\api\dist\mcp.js` line); `npx @modelcontextprotocol/inspector node apps\api\dist\mcp.js` for a hands-on session. | build session |

| 2026-09-05 | Gate 3 · **S5 built** (web: Ask + Funnel + Events + History + Projects + Health) | six gates green for the slice: typecheck strict (contracts + api + `apps/web` app and node tsconfigs); eslint (now linting `.tsx`) + lint-deps (192 files; `apps/web/src` imports `@vantage/contracts` only) + lint-sql; **92 web component/unit tests** across 19 files (Vitest + jsdom + React Testing Library) and **5 Playwright e2e** (Chromium) driving F1/F2/F3/F5 against the built API on an embedded PostgreSQL 17 with the fixture, alongside the unchanged API/contracts suite; the web coverage gate `tools/check-web-coverage.mjs` FAILS the build below its floors (pure helpers `format`/`status`/`funnel`/`highlight` 90/80 lines/branches, components & impure hooks 70/60) — measured lib 96.7/87.6, components 88.2/85.4; adversarial pass done; true headers on every source file. **Delivered:** `apps/web/{package.json,index.html,tsconfig.json,tsconfig.node.json,vite.config.ts,playwright.config.ts,vite-env.d.ts}`, `src/{main.tsx,App.tsx,routes.ts}`, `src/ui/{tokens.css,app.css}` (ported from `docs/prototype/vantage.html`; tokens verbatim from 03-UI §2.2), `src/api/client.ts`, `src/lib/{format,status,funnel,highlight,router,theme,hooks}.ts`, `src/state/ProjectContext.tsx`, `src/components/{Icons,Chip,StatusFooter,Sql,CopyButton,FunnelBars,StateCard,ResultView,QueryCard,Rail,CommandBar,WithProject}.tsx`, `src/views/{Ask,Funnel,Events,History,Projects,Health}View.tsx`, `apps/web/test/**` (component/unit + fixtures + net stub), `apps/web/e2e/**` (`global-setup.ts` + F1/F2/F3/F5 specs); `vitest.web.config.ts`, `tools/{dev,check-web-coverage}.mjs`; root scripts `web:dev|build|preview`, `test:web`, `coverage:web`, `e2e:web`, `check:web`, `dev` (API + web via `tools/dev.mjs`), and `check` now ends with `check:web`; `eslint.config.mjs` lints `.tsx`; `.github/workflows/ci.yml` installs Chromium before `npm run check` and runs `test:web` on Windows. **Screens → flows → status:** S1 Ask (F1 ask→spec→SQL→funnel→`● Complete`; F2 hostile→red `⊘ Refused`+raw output+History row; F3 Edit spec→re-run→SQL diff), S2 Funnel (pickers from `list_events`, order/window/range, collapsible SQL panel), S5 Events (names + properties; no raw-events panel — the catalog carries no values), S6 History (`GET /v1/asks`, expand → raw · spec · SQL), S7 Projects (create + one-time key + curl/PowerShell ingest snippet), S9 Health + the empty≠error≠loading trio (F5); the §2.3 vocabulary is one tested module and all six statuses render distinctly; `empty` ≠ `timed_out` ≠ error ≠ loading are four different DOM shapes; every number carries `meta` and a 200 is never read as success. **What the adversarial pass surfaced/decided:** (1) a re-run that times out replaces the previous funnel — no stale number under a "Stopped" footer (unit test); (2) the card says "the query that ran", never "an answer to your question"; (3) a network error mid-ask → the red error card with the real message + Retry, never a hang (`ApiError`); (4) the funnel window is a **bound parameter**, so Edit-spec→re-run moves the `$n` in the params line, not the SQL text — the diff-highlight path is unit-tested with a text change, and F3 asserts the new bound value `'14 days'`; (5) S5 renders no unbounded table (catalog ≤500, `/v1/asks` ≤200, funnel ≤10 steps), so the 10 000-row cap surfaces as the `truncated` chip rather than needing virtualisation. **Fixed while building:** vite preview bound `localhost`/`::1` and the IPv4 e2e/API could not reach it → `vite.config.ts` binds `127.0.0.1`; two e2e selectors matched the rail's "Ask" button and a `"3"` substring → scoped to `role=search` and to a row. **LLD extensions:** LLD §11 **E62–E68**. **Not done (S6):** Retention and Paths screens (03-UI S3/S4) and flow F6, the retention heatmap, the prototype's raw-events and reveal/rotate panels (no honest API), and a SQL-text diff for a window change (parameterised by design). **Verify (PowerShell):** `cd D:\code\Vantage; npm install --legacy-peer-deps; npx playwright install chromium; npm run check` (typecheck · lint · api coverage · web coverage · web e2e). Live: `npm run build; npm run migrate; npm run fixture:load`, then `npm run dev` and open `http://127.0.0.1:5174` — F1 type "Of the people who signed up in August, how many created a project within a week, and how many of those invited someone?" → spec → SQL → funnel bars 13 → 6 → 3 → `● Complete`; F2 type "drop the events table" → red `⊘ Refused` + raw output, then History shows the row; F3 Edit spec → 14 days → Re-run. | build session |

| 2026-09-05 | Gate 3 · **S4 review** (hostile review of the MCP server) | No high/medium defect: the read-only boundary (every MCP tool reaches only `QueryRunner`→`PG_RO`, no path to `PG_RW` or an `LlmPort`, proven by `mcp-boundary.spec` which greps the module and self-checks on a synthetic violator), the grammar boundary (`sql` key / kind mismatch / 11 steps / 367 days / U+0000 all → `INVALID_SPEC`, never a 500; `project` bound to the caller as `$1`, V14 on data), stdio purity (nothing but JSON-RPC on stdout; boots via `createApplicationContext`, no HTTP, no model module; graceful shutdown), the boot self-test (a mis-granted `vantage_reader` refuses the MCP boot too), and the error contract (unknown tool → −32601 naming no other tool; `NOT_FOUND` with no enumeration; `NOT_IMPLEMENTED` for trend/paths; `BUSY` under a 200-call flood, none hanging) all held. `explain_query`'s plan gate is fail-safe (`=1` on; `''`/`=0`/unset off; anything else throws at boot). Installer merges/backs-up/atomic-renames `claude_desktop_config.json`, least-privilege env (no owner URL), masks passwords, `--dry-run` writes nothing. **Follow-ups deferred to S8 hardening:** (F2) `run_trend`/`run_paths` always throw `NOT_IMPLEMENTED`, so their output-schema and `renderText` branches are runtime-dead until S6 — drop the dead branch or leave as wire-documentation; (F3) the "no imperative in a tool description" test is a denylist — add a positive shape assertion or snapshot-pin the eight description strings so a future imperative (e.g. "First call list_projects, then …") cannot regress unnoticed. Fixed now: the S4-built row above said tools use `ProjectsService` — corrected to `McpReads` (the RO-pool reader; `ProjectsService` uses the write pool and is deliberately not imported). | review session |

| 2026-09-06 | Gate 3 · **S6 built** (trend, retention heatmap, paths — the last query kinds and the last three query screens) | six gates green: typecheck strict; eslint + lint-deps (218 files) + lint-sql (21 domain files); the full API suite **648 tests / 54 files** on embedded PostgreSQL 17 (incl. the new `test/integration/insights-s6.spec.ts` 18 and `attacks-s6.spec.ts` 6, and the extended `compile.spec.ts`), the web suite **27 files** under `vitest.web.config.ts`, and **7 Playwright e2e** (F1/F2/F3/F5 + **F6a/F6b**); the API two-layer coverage gate stays alive and every LLD §7 floor is met (domain 100/94.8, modules 99.3/89.0, infra 98.2/90.9, contracts 99.5/100 lines/branches), the web gate met (helpers 90/80, components & hooks 70/60); adversarial pass = the S6 rows as `attack:` tests; true headers on every new file. **Delivered:** `apps/api/src/domain/compile/{trend,paths}.ts` (+ `index.ts` dispatch now total over five kinds), `packages/contracts/src/results.ts` (`TrendResult`/`TrendPoint`/`TrendSeries`, `PathsResult`/`PathsTransition`; `QUERY_LIMITS.pathsTransitions = 50`), `contracts/{ask,mcp}.ts` (the two results join `InsightResult`; `NOT_IMPLEMENTED`/`NotImplementedOutput` removed, `run_trend`/`run_paths` output schemas are now the real results), `modules/insights/{insights.service,insights.controller}.ts` (`POST /v1/trend|paths`, `trend()`/`paths()`, `UnsupportedKindError` deleted), `modules/mcp/{mcp-tools,mcp-text,mcp-errors}.ts` (`run_trend`/`run_paths` run through the same service; trend/paths renderers), `modules/ask/ask.service.ts` (no kind is refused as unsupported now), `apps/web/src/components/{RetentionHeatmap,TrendChart,PathsTable,DegradedResult}.tsx`, `apps/web/src/views/{RetentionView,TrendView,PathsView}.tsx`, `apps/web/src/lib/{retention,paths,trend,range}.ts`, `routes.ts`/`App.tsx`/`Icons.tsx`/`api/client.ts`/`ui/app.css` (nine-screen rail, ported heatmap/paths/trend CSS), `apps/api/fixtures/august.expected.md` (hand-computed trend + paths). **Invariants proven:** the fixture's `signup` daily trend (11 buckets, 14 events, and unique-persons equal because P07's two signups fall on Aug 8 and Aug 20) and `view_pricing` daily trend (Aug 16 = **1 000 events / 1 person**, the events-vs-persons discriminator) by name; paths from `signup` (30-min gap, 5 steps) = **8 transitions from 14 start walks**, led by `signup → view_pricing` (count 2, median gap 930 s), P14's session bounded to five steps; the **sessionisation boundary** (30 min = same session, 31 min = new session); **top-50 truncation** returns 50 and reports `total_transitions = 60`; `timed_out`/`empty` honesty; V14 on two projects with identical event names; `run_trend`/`run_paths` return the SAME numbers and SQL as HTTP, unknown project → `NOT_FOUND`, still `readOnlyHint`; the web heatmap's global scale + hatched in-progress cells, the trend chart's dashed tail, the paths top-N chip, all as component tests; F6 against the real API. **Adversarial pass (LLD §9 S6):** a paths query built to session-explode stays bounded to 5 steps (2 000-event session → complete, `starts = 1`); a trend breakdown on a 1 M-cardinality key caps at 50 named + "other" (or `timed_out`, `result: null`); U+0000 in a trend event / paths start → 422 `INVALID_SPEC`, never a 500; a 367-day range refused before any SQL. **What the pass surfaced and fixed:** (1) `row_number()` is bigint → arrived as a string, so `step` compared `!== 1` in the UI — cast to int in the compiler; (2) the builders seeded their date range from the catalog span, and the fixture's stray **2025** view_pricing made that span > 366 days, so every first Run 422'd — `src/lib/range.ts` `catalogRange` now clamps to the newest 366 days (E74; the F6 e2e caught it). **LLD extensions:** §11 **E69–E75** (added to §11; §8 S6 row marked built). **Not done (S7/S8):** the seed generator and the `anthropic`-costed adapter (S7); CI wiring the full gate, the README/DESIGN polish and the 1 M bench (S8); the MCP web screen (S8) is the one 03-UI screen still deferred. **Verify (PowerShell):** `cd D:\code\Vantage; npm install --legacy-peer-deps; npx playwright install chromium; npm run check` (typecheck · lint · api coverage+gate · web coverage · web e2e incl. F6). Live: `npm run build; npm run migrate; npm run fixture:load` (prints the project id), then `npm run start:api` and, in another window with `$p='<project id>'`: `Invoke-RestMethod -Method Post -Uri http://127.0.0.1:4100/v1/trend -ContentType application/json -Body (@{kind='trend';project=$p;range=@{from='2026-08-01';to='2026-08-31'};event=@{event='signup'};measure='events';unit='day'} \| ConvertTo-Json -Depth 6) \| ConvertTo-Json -Depth 8` (11 buckets summing to 14) and `Invoke-RestMethod -Method Post -Uri http://127.0.0.1:4100/v1/paths -ContentType application/json -Body (@{kind='paths';project=$p;range=@{from='2026-08-01';to='2026-08-31'};start='signup';steps=5} \| ConvertTo-Json -Depth 6) \| ConvertTo-Json -Depth 8` (8 transitions, `signup → view_pricing` first). Web F6: `npm run dev`, open `http://127.0.0.1:5174`, key **3** (Retention) → set Periods 30 → Run → the heatmap with hatched recent cells + hover tooltip; key **4** (Paths) → steps 5 → Run → the ranked table with the top-N chip. | build session |

| 2026-09-06 | Gate 3 · **S8 built — Vantage is FEATURE-COMPLETE** (the web setup + MCP screens, the portfolio docs, real CI + a performance bench) | six gates green: typecheck strict; eslint + lint-deps (230 files) + lint-sql; the full API suite green on embedded PostgreSQL 17 (coverage gate alive, every LLD §7 per-area floor met — contracts 99.47/100, domain 100/94.84, modules 99.35/89.12, infra 98.20/90.87 lines/branches); the web suite **135 tests / 29 files** (web per-area floors met: helpers 90/80, components & hooks 70/60); **11 Playwright e2e** (F1–F3, F5, F6a/b, and the new **F7/F8/F9**); adversarial pass done; true headers on every new file. **Delivered — API:** a rotate-key route (`POST /v1/projects/:id/rotate-key`, `ProjectsService.rotateKey`, contract `RotateKeyResponse = ProjectCreated`) that mints a NEW key and returns it once — it CANNOT reveal the stored key (only its sha256 exists), so this overturns the E67 note "there is no rotate route" honestly: rotation replaces the credential and the old key stops authenticating (proven by an integration test: old key → 401, new key → 200); the column-level grant `projects UPDATE(api_key_hash)` was added to `grants.sql` and to the boot self-test allowlist (`self-test.ts`), so `vantage_app` still cannot change a project's name or timezone. **Delivered — web:** `src/views/McpView.tsx` (the one 03-UI screen deferred from S5 — the `claude_desktop_config.json` block and the `claude mcp add` one-liner built by `src/lib/mcp.ts` the way `tools/mcp-install.mjs` builds them; the eight tools rendered from the `MCP_TOOLS` contract with `readOnlyHint` badges so the list cannot drift from the server's; a short transcript of Claude calling `list_events` then `run_funnel`, labelled "example — not a live call" and again "Illustrative"); `ProjectsView.tsx` gains the rotate control, a JS snippet tab (curl/PowerShell/JS), and a live **Try it** panel that posts a fixed batch through the real `POST /v1/events` (F7: accepted/duplicate counts render; a second send dedupes) and merges the two demo persons via `POST /v1/identify` (F8: the merge notice) — the key travels in the `Authorization` header, never a URL (`client.ts` `authedPostInit`, new `ingest`/`identify`/`rotateKey`); `routes.ts` gains `mcp` so the rail now holds all ten 03-UI screens (keys 1–9 then `0` for Health), `App.tsx`/`Icons.tsx` (`i-mcp`, `i-merge`) and `ui/app.css` (ported §12 CSS: `.resp`/`.tools`/`.badge`/`.transcript`/`.msg`/`.toolcall`) updated. **Delivered — docs:** **`DESIGN.md`** at the repo root (the LLM-to-SQL security argument, self-contained from design §4 — the threat, the four-layer boundary, the "drop the events table" trace, what a hostile model can/cannot cause), **`docs/DEMO.md`** (the 90-second script with exact commands incl. the `psql -U vantage_reader` denial and the MCP path), README final polish (links `DESIGN.md`/`DEMO.md`, status → feature-complete, the `mcp:install`/CI-bench notes made real, the stale "planned"/"remaining slices" claims removed), `tools/check-docs.mjs` now validates `DESIGN.md`'s links too. **CI decision (measured, honest): 200 k in CI, 1 M documented as a local stress command.** New `apps/api/test/bench/bench.spec.ts` + `vitest.bench.config.ts` + `npm run bench` + a CI **bench job** (ubuntu + postgres:17) run the LLD §7.4 funnel/retention/paths queries and FAIL past 2× a scaled budget. Measured on this laptop's embedded PostgreSQL 17: at **200 k** events the ÷5 budgets (0.4/0.6/0.6 s; 2× = 0.8/1.2/1.2 s) hold with headroom — **funnel 216 ms, retention 319 ms, paths 633 ms**; at **1 M** they do NOT — **funnel 1 358 ms, retention 1 454 ms, paths 3 091 ms** — because query time is not linear in row count and the 10 M budgets assume production hardware. So CI runs the 200 k variant (the real regression guard) and the documented 1 M local run (`$env:VANTAGE_BENCH_EVENTS='1000000'; npm run bench`) asserts only the hard promise every size must keep: completion inside the reader's 5 s `statement_timeout`. Nothing about the bench is hand-copied — the spec prints `measured:` lines and asserts them. **Adversarial pass:** the rotate flow never shows a stored key it cannot have (it returns a freshly minted key; the old one 401s — tested); the MCP transcript is static and doubly labelled illustrative (asserted by the component test and F9); the ingest snippet and the live Try-it both use the real key path with the key in the `Authorization` header (never a query string); the MCP config JSON carries only placeholders, never a real secret (unit-tested). **Not done (by design):** no live Claude Desktop registration is exercised in CI (the installer is unit-tested against a temp APPDATA; the Inspector/Desktop check is manual — E60); no real Ollama/Anthropic call (fake-server-tested, S3); a browser cannot read this machine's absolute node path, so the MCP screen shows the installer's SHAPE with labelled placeholders and directs the operator to `npm run mcp:install`. **LLD extensions:** §11 **E79–E83**. **Verify (PowerShell):** `cd D:\code\Vantage; npm install --legacy-peer-deps; npx playwright install chromium; npm run check` (typecheck · lint · api coverage+gate · web coverage · web e2e incl. F7–F9); `npm run bench` (200 k, green) and `$env:VANTAGE_BENCH_EVENTS='1000000'; npm run bench` (1 M local stress); `node tools/check-docs.mjs` (validates README + DESIGN.md + docs links). Live: `npm run build; npm run migrate; npm run fixture:load; npm run dev`, open `http://127.0.0.1:5174`, key **8** (Projects) → Create project → the one-time key + Rotate + Send test batch (accepted 3, then send again → duplicates 3) + Identify (merge notice); key **9** (MCP) → the config JSON, the eight readOnlyHint tools, and the illustrative transcript. | build session |
| 2026-09-06 | Gate 3 · **S7 built** (the seed — the demo's deterministic ~200 k-event dataset; the web S7/S8 setup screens and the `anthropic` adapter are not part of this row: the adapter shipped in S3, and the setup screens remain for a later session) | six gates green: typecheck strict (now also `tools/tsconfig.json`, so `tools/seed.ts` is type-checked against `apps/api/dist`); eslint + lint-deps (223 files) + lint-sql; the full API suite **657 tests / 55 files** on embedded PostgreSQL 17, incl. the new `test/integration/seed.spec.ts` (9); the two-layer coverage gate stays alive and every LLD §7 per-area floor plus the global 90/80/90/90 hold — the new `apps/api/src/seed/generate.ts` measures **100 / 95.45** (lines/branches); adversarial pass = the LLD §9 S7 rows as tests; true headers on the two new source files. **Delivered:** `apps/api/src/seed/generate.ts` (PURE generator — one seeded mulberry32 PRNG, returns a `Fixture` so it plays through the same `playFixture`/real ingest path the hand-computed fixture does, anchored to fixed August 2026 dates so it is identical across runs, stable counter-minted `insert_id`s), `tools/seed.ts` + `npm run seed` (`node --experimental-strip-types`, boots the API in-process like `fixture-load.ts`, posts every batch through `POST /v1/events`/`POST /v1/identify`, creates or reuses a `Demo` project via a git-ignored `.seed-state.json`, prints the id + counts + a ready-to-paste funnel query, `--events`/`--signups`/`--seed`), `tools/tsconfig.json`, the `seed` script, `.gitignore` `.seed-state.json`, README seed note rewritten, LLD §11 **E76–E78** and the §8 S7 seed row marked built. **Invariants proven** (`seed.spec.ts`, reduced 5 000-event scale): funnel monotonic + non-trivial; retention > 1 day cohort; a replay adds zero rows (accepted 0 / duplicates = submitted); an interrupted (prefix) seed re-run to completion doubles nothing (prefix dedupes, remainder accepted, final count = full unique total); the skewed device is `ts_source = client_shifted`; the late arrivals bucket in the first two August days despite being ingested last; re-generating at a different `serverNow` yields byte-for-byte identical events. **Adversarial pass:** re-running the seed is a no-op (proven live AND in the suite); a partial/interrupted seed leaves no dupes on the next run (the prefix-then-full test); the skewed and late events are handled honestly (their own tests). **Design points made real:** the CLI cannot recover an existing project's key (no rotate route, E67), so idempotent reuse needs the one-time key remembered locally — `.seed-state.json`, the same loopback-only trust model `.env` (DB passwords) and the MCP installer (E60) already use; the dataset is anchored to a fixed "today" (2026-09-05 newest event), not the wall clock, so a re-run minutes or months later still dedupes. **Measured** (live, built `dist`, throwaway embedded PostgreSQL 17): full seed **198 666 events / 5 004 persons / 28 day cohorts** through the real ingest path — first run 198 666 accepted, 0 duplicates in **~7.5 s**; a second run reused `Demo` and reported **0 accepted / 198 666 duplicates in ~4.5 s**; funnel `signup 5 004 → create_project 2 364 → invite_teammate 1 067`. Reduced-scale test: 5 129 events / 141 persons. **Not done:** the web S7/S8 setup screens (03-UI) and flows F7–F9; a real Ollama/Anthropic call (the adapter is S3's, fake-server-tested); the S8 items (CI full gate, README/DESIGN polish, 1 M bench). **Verify (PowerShell):** `cd D:\code\Vantage; npm run typecheck; npm run lint; npm run coverage` (the seed test is in the suite); live: `npm run build; npm run migrate; npm run seed` (prints the Demo project id and a funnel query; run it again to watch every event dedupe), then `npm run start:api` and paste the funnel line the seed printed, e.g. `Invoke-RestMethod -Method Post -Uri http://127.0.0.1:4100/v1/funnel -ContentType application/json -Body (@{kind='funnel';project='<printed id>';range=@{from='2026-08-01';to='2026-08-31'};steps=@(@{event='signup'},@{event='create_project'},@{event='invite_teammate'})} \| ConvertTo-Json -Depth 6)` (≈ 5 004 → 2 364 → 1 067). Reduced/fast: `npm run seed -- --events 5000`. | build session |
| 2026-09-06 | Gate 3 · **Trim pass** (subtractive minimize pass; no behaviour change) | Baseline green first (typecheck; lint = eslint + lint-deps + lint-sql; `npm run coverage` = api 96.05% stmts / 90.75% br / 97.04% lines with every §7 per-area floor met; `npm run check:web` = web coverage 92.62% stmts / 94.39% lines + 11/11 e2e). Ran `knip` and `ts-prune` transiently (not added to package.json). **Finding: the tree is already minimal — no genuinely dead code.** Every scanner candidate resolved to load-bearing or a false positive: the 3 "unused files" (`mcp.ts`, `migrate.ts`, `fixture-load.ts`) are entry points run from `dist` via package.json scripts / `mcp-install` / stdio tests; `jsdom` is the web test `environment`, not an import; `RotateKeyResponse` is an intentional wire-contract alias of `ProjectCreated` (consumed by controller, service, web client); the compile `*Data` type re-exports are the domain's public return types; and every other "unused export" is a symbol used *inside its own module* that merely didn't need the `export` keyword. No commented-out code, no TODO/FIXME/dead branches, no duplicate helpers, no unused imports (eslint enforces), no unused devDeps. **Removed:** the redundant `export` keyword on five purely-internal, non-seam, non-test-imported, non-documented symbols only — `JSON_BODY_LIMIT` (app.ts), `DEMO_RANGE` (llm/none.ts), `OLLAMA_DEFAULT_URL` (llm/ollama.ts), `FIXTURE_PATH` (fixture/play.ts), `DEFAULT_ROUTE` (web routes.ts); each is still defined and used, only the module's public surface shrank. **Kept (scanner-flagged) on purpose:** `DEFAULT_LLM` and `RW_POOL_MAX` (documented in LLD), all `arb*`/`matches`/`addUnits` test helpers (live via `arbAnySpec`/internal use; LLD §7.3 seam), all contract/domain re-exports and entry points. Re-verify: `npm run check` green end-to-end (exit 0), same coverage gates, e2e 11/11. Net: 0 lines/files/deps removed, 5 export keywords narrowed. | trim pass (agent) |
| 2026-09-06 | Gate 3 · **Perf pass** (correctness-preserving speed-up of the paths hotspot; measured at 1 M events on an isolated embedded PostgreSQL 17) | **Method: measure → optimise only a proven hotspot → re-measure.** Profiled funnel / retention / paths with `EXPLAIN (ANALYZE, BUFFERS)` on a generated 1 M-event / 50 k-person dataset (the bench harness's SQL generator) as the reader's cost. **Paths was the slow spot** (~2.28 s warm, ~2.8 s cold). The plan proved the cost was **not** the scan (BRIN bitmap heap scan ≈ 200 ms) but the `after_start` self-join: `sessionised ⋈ first_start` merged on `person_id` **only** — `session_id` and the `(event_ts, event_id) >= (start_ts, start_id)` row-value compare fell to a Join Filter — so every event was cross-multiplied by every session of the person: **8.67 M rows removed by the filter, a 9.1 M-row Materialize.** A candidate covering index `events (project_id, event_ts, event_id) INCLUDE (distinct_id, event)` was built and tested; the planner did **not** choose it and it gave **no** improvement, so **no index and no migration 0005 was added** (adding a useless index was explicitly avoided). **Change (`apps/api/src/domain/compile/paths.ts` only):** the `first_start` CTE and the self-join are replaced by one window pass — `after_start` keeps a session's rows where a running `count(*) FILTER (WHERE event = start)` over the **same** `(event_ts, event_id)` order the walk already uses is `>= 1`; this is identical row-for-row and tie-break-for-tie-break to the old `(event_ts,event_id) >= (first_start_ts,first_start_id)` compare (the first start is the minimum start key, so "a start seen so far" ⇔ "this row's key ≥ that minimum"). `starts` now counts the distinct `(person_id, session_id)` pairs that contain a start — what the old per-session first-start CTE counted. **Params, kinds, result shape, the security boundary (read-only role, parameterisation, `SET LOCAL statement_timeout`, row cap) all unchanged.** Two alternatives (deriving `starts` from `after_start`; folding `last_start_session` into the session-numbering pass) were measured and were **slower** — `count(DISTINCT (person,session))` costs more than the cheap separate `Unique` scan — so the simplest rewrite was kept. **Byte-identical output proven:** captured a golden of the 1 M paths result and asserted `toEqual` before/after; `compile.spec`'s paths SQL snapshot updated (only the paths CTEs changed — asserted params on line 172 unchanged); `insights-s6.spec` + `attacks-s6.spec` (the fixture's hand-computed paths numbers — 8 transitions, `signup → view_pricing` first, the 30-min sessionisation boundary, top-50 truncation, P14 bounded to 5 steps) all green unchanged. **Measured (same-process A/B, 1 M events, warm min-of-3):** paths **2 280 ms → 2 088 ms (~8 %)**; funnel ~1.23 s and retention ~1.40 s were already well within budget, their plans showed no clean win, and they were left untouched. The larger benefit is structural: the O(events × sessions/person) cross product is gone, so paths scales linearly rather than super-linearly. **Verify:** `cd D:\code\Vantage; npm run check` green (659 API tests / 55 files, coverage gate alive + every §7 floor met, web coverage + 11/11 e2e); `$env:VANTAGE_BENCH_EVENTS='1000000'; npm run bench` completes inside the reader's 5 s `statement_timeout`. Net: 1 source file changed, 1 snapshot updated, 0 migrations, 0 indexes. | perf pass (agent) |
| 2026-09-07 | Gate 3 · **Deploy** (one hostable web app + the query-auth gate; no behaviour change when unconfigured) | **Part A — the security gate (real code + tests).** New env `VANTAGE_QUERY_TOKEN` (validated in `config.ts` ≥ 16 chars, threaded through `AppModule.forRoot` → a `@Global` `AuthModule.forRoot` providing `QUERY_TOKEN` + `QueryTokenGuard`; documented in `.env.example`). **Unset/empty ⇒ today's exact behaviour** — the read routes stay open ⟨D4⟩, the loopback default unchanged. **Set ⇒** `QueryTokenGuard` requires `Authorization: Bearer <token>` on the READ controllers only — insights (`/v1/funnel|retention|trend|paths|count`), ask (`/v1/ask`), audit (`/v1/asks`), events (`/v1/events/catalog`) — 401 `INVALID_QUERY_TOKEN` (standard `{code,message}` via `http-error.filter`) for missing/wrong. The guard reuses the exported `BEARER` parse and constant-time-compares SHA-256 digests (no length leak, token never logged). **Ingest is untouched** — its per-project `ApiKeyGuard` is independent; a project key is not a query token and vice-versa. `/health` is intentionally left open (orchestrator probes; no analytics data). `POST /v1/projects` stays open as under D4 (documented in DEPLOY.md as a residual for public hosting). **Tests:** `test/integration/query-token.spec.ts` (8) — unset ⇒ 200 on every read route; set ⇒ 401 without / 401 wrong / 200 correct across funnel/count/ask/asks/catalog; ingest still works on its own key; the two mechanisms independent; V7 boundary intact (authorised "drop the events table" still refused, query still runs as `vantage_reader`); health not gated — plus `config.spec.ts` (+2). **`npm run check` green: 669 tests / 56 files** (up from 659), typecheck strict, lint (eslint + lint-deps 233 files + lint-sql), every §7 coverage floor met, web coverage + 11/11 e2e. **Part B — one image, one URL.** `Dockerfile` (multi-stage: build API + web, runtime `node:22-slim` + static `caddy:2-alpine` binary), `Caddyfile` (serves the SPA + `reverse_proxy` `/v1/*` and `/health` → loopback API on 4100, unchanged ⟨D4⟩), `docker-entrypoint.sh` (API in background, Caddy PID 1 on `${PORT:-8080}`; relies on the boot migration when `VANTAGE_DATABASE_URL_OWNER` is set), `docker-compose.yml` (`postgres:17` + app, first-boot roles/db from `tools/db-setup.sql` via `tools/docker-initdb.sh` mounted into `/docker-entrypoint-initdb.d/`, only the Caddy port published), `fly.toml` (`internal_port` = 8080, `force_https`, `auto_stop_machines=false`, `min_machines_running=1`, secrets via `fly secrets`), `.github/workflows/release.yml` (push to main → build+push `ghcr.io/abheet19/vantage:{latest,sha}` via `${{ github.token }}`; a `gate` job maps `secrets.FLY_API_TOKEN` to an output and a `deploy` job runs `flyctl deploy` only when it exists — `ci.yml` untouched), `.dockerignore`, `docs/DEPLOY.md`. **Verified live:** `docker compose build` OK, **image 484 MB**; `docker compose up -d` → Postgres healthy, migrations applied, boot self-test `ok:true` (RW=vantage_app, RO=vantage_reader — the read-only boundary is intact, the gate sits in front of it); `fixture:load` in-container = **1046 submitted / 1045 accepted / 1 duplicate**; through the proxied edge (`:8080`): funnel **without** bearer → 401 `INVALID_QUERY_TOKEN`, **with** the token → 200 and **persons [13, 8, 4]**, median **91 500 s** — an exact match to `fixtures/august.expected.md` ("14 days: 13 → 8 → 4"); ingest without a key → 401, with its project key → 200, with the query token → 401 `INVALID_API_KEY` (independent); web dashboard HTML (`<title>Vantage</title>`) served at `/` with SPA deep-link fallback; the API's 4100 is NOT published (only 8080 maps to the host; Postgres 5432 is network-internal). `docker compose down -v` after. **Net:** Part A code = 1 new guard + 1 module, `config.ts`/`app.ts`/`main.ts`/`tokens.ts` threaded, 4 read controllers gated, `.env.example` + 2 specs; Part B = 8 new deploy files. No `git commit`/`push` (owner reviews + pushes). | deploy pass (agent) |

Write approvals here as `YYYY-MM-DD · Gate N · approved / changes: …`. The build session reads this
table first and refuses to write code if Gate 2 is not marked approved.

## Standing rules for every session

- NEVER `git commit` or `git push`. Edit the working tree; the owner reviews and lands everything.
- No AI-attribution trailers in commit messages.
- Budget is $0 to run. Anything paid arrives as a costed proposal with a free alternative.
- Windows 11, Node 22, PowerShell. Give PowerShell commands (`cd D:\code\Vantage`).
- Solo developer, ~8–10 hours a week, interviewing at the same time. Slice accordingly.
- LLD before code. Signature and doc comment before body. Tests alongside. If a line cannot be
  explained, delete and rewrite it.
- Vantage stands alone. No imports from, dependencies on, or framing as part of Zeno or Weft.

## The Gate 3 build prompt (paste into a fresh session once Gate 2 is approved)

```
The Vantage design and LLD are both approved. Build it one slice at a time, in the LLD's order. Do
not skip ahead. Do not start slice 2 until slice 1 is green and I have seen it.

Read the design and the LLD first and treat both as binding.

## Rules for every slice
- LLD BEFORE CODE. If the approved LLD does not cover what you are writing, extend it and show me
  the extension first.
- Signature and doc comment BEFORE the body. Comments explain WHY, never what.
- Tests alongside, never after.
- Every invariant gets a real test. Funnel and retention correctness are tested against a fixture
  dataset with HAND-COMPUTED expected numbers — a plausible wrong number is the worst possible
  failure in analytics, and it will not announce itself.
- Every slice ships the denied path and the interrupted path.

## The six gates every slice passes
1. Typecheck clean, strict, no unargued `any`.
2. Lint clean.
3. All tests green.
4. Coverage gate that FAILS the build, not a report.
5. AN ADVERSARIAL PASS: attack the slice as a hostile reviewer before calling it done. For the LLM
   path specifically, actively try to make it emit a write. THIS IS NOT OPTIONAL — on my other
   project six real vulnerabilities sat under a fully green suite, including an endpoint that handed
   the admin token to any unauthenticated local caller.
6. I can explain every line.

## Non-negotiables
- Budget $0 to RUN. Postgres or SQLite locally. Windows 11, Node 22, PowerShell commands.
- NEVER git commit or git push.
- The LLM boundary is STRUCTURAL, never a prompt instruction. A read-only role, a constrained
  grammar, a statement allowlist, limits and a timeout. If the only thing standing between a user
  and DROP TABLE is wording in a system prompt, the design has failed.
- HONEST DEGRADATION: "no events matched" and "the query timed out" are different facts and must
  never render the same.
- NEVER show a number you cannot defend. If a result is partial, approximate, or computed over an
  incomplete window, the UI says so. Analytics that quietly lies is worse than analytics that
  refuses.
- NO INVENTED NUMBERS in the README or badges. Wire real CI so a badge is earned, or omit it.

## Project-level deliverables — these ARE the portfolio
- A README an interviewer reads in 3 minutes: the problem, the one hard idea (the structural LLM
  boundary), a Mermaid diagram, install, and an honest "what this does not do yet".
- A DESIGN.md capturing the LLM-to-SQL security argument — the single most interview-valuable
  document in this project.
- A 90-second demo: ask a plain-English question, SEE THE SQL, then watch a hostile question get
  refused structurally.
- Real CI running the full gate.

## When you think a slice is done
Show me: what you built, the tests and what each proves, what you attacked and what broke, what you
deliberately did NOT do, and the exact PowerShell commands for me to verify it. Then stop and wait.

## Additional binding instruction from Gate 1
If the prototype in docs/prototype/vantage.html is approved, PORT its markup and CSS and rewire it
to live data. Do not re-invent the information architecture.
```
| 2026-09-07 | CI fix | **Three red-CI failures addressed.** (1) `.github/workflows/ci.yml`: both postgres service jobs reverted from `POSTGRES_HOST_AUTH_METHOD: trust` to real password auth (`POSTGRES_PASSWORD: postgres`; `VANTAGE_TEST_DB: postgres://postgres:postgres@127.0.0.1:5432/postgres`; each with `# pragma: allowlist secret`). Trust auth ignored role passwords, so the deliberately-wrong reader password in mcp-stdio "a refused boot" was accepted and the boot never refused → the test hung. Local docker-compose trust auth for dev is deliberately unchanged. (2) `tools/check-coverage-gate.mjs`: per-area matching now normalises `\`↔`/` and strips the repo root by string prefix instead of OS-specific `isAbsolute`/`relative`, so backslash-path summaries match the posix area globs on Linux (check-coverage-gate.spec is OS-independent again). (3) attacks.spec `person_merges`: investigated, NO change — the merge path only INSERTs into `person_merges` (granted) and the reader footer (`compileMeta`) SELECTs it on the RO pool (granted); the grant set and self-test allowlist are already correct, and adding a grant would (rightly) fail the boot self-test. Verified locally: attacks 23/23, mcp-stdio 5/5, check-coverage-gate 5/5. |
| 2026-09-07 | Gate 3 · **project-admin write gate — `VANTAGE_ADMIN_TOKEN`** | Added an env-flagged shared-bearer gate on the project-ADMIN WRITE routes, mirroring `VANTAGE_QUERY_TOKEN`/`QueryTokenGuard` exactly, so a public deployment is not an open project-admin surface while the read surface stays open for a read-only demo. **Unset/empty ⇒ today's behaviour (routes open, loopback default unchanged ⟨D4⟩); set (≥ 16 chars) ⇒** `POST /v1/projects` (create) and `POST /v1/projects/:id/rotate-key` (rotate key) require `Authorization: Bearer <VANTAGE_ADMIN_TOKEN>`, missing/wrong → `401 {code:'INVALID_ADMIN_TOKEN', message}` via the existing http-error filter. It is a single shared admin token, INDEPENDENT of both `VANTAGE_QUERY_TOKEN` (reads) and the per-project ingest `ApiKeyGuard`; constant-time SHA-256 compare (the exported `sameToken` reused from `QueryTokenGuard`), read only from the Authorization header, never logged. **Gated exactly two routes:** create + rotate-key. **Left open (verified by test):** ingest, identify, the read/insights/ask/audit/events routes, `GET /v1/projects` (list — exposes only ids/names, needed by the dashboard), and `GET /health`. **Files added:** `apps/api/src/modules/auth/admin-token.guard.ts`, `apps/api/test/integration/admin-token.spec.ts`. **Files changed:** `infra/tokens.ts` (`ADMIN_TOKEN` provider token), `infra/config.ts` (`adminToken` field + ≥16-char validation, empty⇒undefined), `modules/auth/query-token.guard.ts` (export `sameToken`), `modules/auth/auth.module.ts` (@Global — provide/export `ADMIN_TOKEN` + `AdminTokenGuard`, second `forRoot` arg), `modules/projects/projects.controller.ts` (`@UseGuards(AdminTokenGuard)` on the two write methods only), `app.ts`/`main.ts` (thread `config.adminToken`; boot warns off-loopback with no admin token and logs the routes' state), `test/helpers/app.ts` (`adminToken` option, `adminBearer` helper, `createProject` sends the admin bearer when set), `test/unit/config.spec.ts` (admin-token defaulting/validation/independence). **Docs:** `.env.example`, `docker-compose.yml` (app env, optional/unset by default), `docs/DEPLOY.md` (new "project-admin gate" section + Fly secrets step; note that with it set the dashboard's create/rotate controls 401 for visitors — intended for a public read-only demo). No web-app code change. **Tests:** admin-token.spec 7/7 (unset⇒create+rotate open; set⇒401 no-header/401 wrong/200-201 correct on BOTH; three mechanisms independent; ingest authenticates on its own key; read routes + list + /health open regardless); full `npm run check` green — typecheck, eslint+lint-deps(235)+lint-sql(21), api unit+integration 609/609, coverage gate alive, web coverage, 11 Playwright e2e (incl. F7/F7b create+rotate against the default open app). The self-test/privilege matrix (DB grants) is untouched. **Verify (PowerShell):** `cd D:\code\Vantage; npm run check`; the gate alone: `npx vitest run --project api-integration test/integration/admin-token.spec.ts`; live: `$env:VANTAGE_ADMIN_TOKEN='<32+ hex>'; npm run start:api` then `POST http://127.0.0.1:4100/v1/projects` with no header → 401 INVALID_ADMIN_TOKEN, with `Authorization: Bearer $env:VANTAGE_ADMIN_TOKEN` → 201; a read (`POST /v1/count`) with no header → 200. NEVER git commit/push. | build session (agent) |
| 2026-09-07 | CI fix · **attacks.spec 50-parallel `read ECONNRESET` on Linux CI** | **Root cause (a test-harness concurrency bug, not an ingest bug).** `attacks.spec` "50 parallel POSTs … leave exactly 500 rows and duplicates summing to 24 500" passed on Windows/embedded but failed fast (~200 ms) on CI (Linux + networked `postgres:17`). Reproduced by running the suite **inside a `node:22` Linux container** against a real `postgres:17` container (Windows-Node → the same container passed — the true axis is Node-on-Linux vs Node-on-Windows TCP, not embedded-vs-networked, which is why `VANTAGE_TEST_DB` alone on Windows never reproduced it). Instrumented run: **33 of 50 requests rejected with `read ECONNRESET`, 17 fulfilled (all 200) — yet the DB held exactly 500 event rows and 50 persons.** So the V1 idempotent-ingest guarantee was never violated; the surviving requests converged the batch. The fault was in `test/helpers/app.ts`: it only `app.init()`-ed the Nest app and handed `supertest(app.getHttpServer())` a **not-yet-listening** server. Supertest then calls `server.listen(0)` itself, once per request, and closes it after — so the 50 concurrent requests raced the same ephemeral bind; Linux reset most of the sockets, Windows tolerated it. **Fix (one line + comment, test-only):** `createTestApp` now `await app.listen(0)` (an ephemeral port, exactly as `main.ts` binds its real one) so supertest connects to an already-listening server and every concurrent request reaches it. **No production, ingest, identity, grant, or schema change** — the ingest advisory-lock create-or-get and `ON CONFLICT DO NOTHING … RETURNING` were proven correct in isolation (ON-CONFLICT never double-counts, advisory lock keeps persons at exactly 50) across ~1 700 raw concurrent iterations up to 120-way; the real invariants (500 rows, 50 persons, accepted 500, duplicates 24 500) are asserted unchanged and hold. **`person_merges` verdict: benign, no change** — the `[http] permission denied for table person_merges` log is `identity.spec`'s "a failure between the repoint and the person_merges insert rolls back all three writes" deliberately running `REVOKE INSERT ON person_merges FROM vantage_app`, asserting the 500, then restoring the grant; it comes from an `/v1/identify` merge, never from the pure-signup 50-parallel test. **Verify:** full `attacks.spec` on Linux/`postgres:17` **fails 5/5 pre-fix, passes 5/5 post-fix**; isolated 50-parallel **10/10** green on Linux; full `api-integration` 293/293 on both Linux (container) and Windows (embedded); `npm run typecheck`, `npm run lint`, `npm run coverage` (669 tests, gate alive) green on Windows. No `git commit`/`push`. | CI fix (agent) |
